import { withMainLayout } from "../../src/components/MainLayout"

function Cluster() {
    return (
      <div>
        <p>
            cluster
        </p>
      </div>
    )
  }

export default withMainLayout(Cluster)